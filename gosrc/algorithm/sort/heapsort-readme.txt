堆是一种特殊的树形数据结构，堆是一颗完全二叉树，根结点的值小于（或大于）两个子结点的值，同时，根结点的两个子树也分别是一个堆。

堆分为大顶堆和小顶堆：
对于给定n个记录的序列(r(1),r(2),……,r(n))，当且仅当满足条件(r(i)>=r(2i),i=1,2,...,n)时成为称为大顶堆，此时，堆顶元素必为最大值。

对于给定n个记录的序列(r(1),r(2),……,r(n))，当且仅当满足条件(r(i)<=r(2i + 1),i=1,2,...,n)时成为称为小顶堆，此时，堆顶元素必为最小值。

堆排序主要包括两个过程：一是构建堆；二是交换堆顶元素与最后一个元素的位置。